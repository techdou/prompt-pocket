# Prompt Pocket 深度代码审查报告

日期：2026-08-08 ｜ 版本：v2.0.3 ｜ 范围：Rust 后端 + Svelte 前端 + 工具库/测试 + CI/配置（约 8300 行源码，100% 覆盖）
方式：三路并行审查 + 交叉验证（`cargo check`、`tsc --noEmit`、`svelte-check`、`node --test` 均实跑验证）

**总计 47 个独立问题：CRITICAL 3 ｜ HIGH 10 ｜ MEDIUM 12 ｜ LOW 22**
（去重后：Editor `$effect` 漏依赖、tsc 红、CDN 供应链、`__KEEP__` 哨兵被两路独立发现，合并计一次）

---

## 一、CRITICAL（数据丢失，必须修）

### C1. 重命名时先删旧文件再写新文件，写失败数据全丢
`src-tauri/src/store.rs:427-430`
```rust
if final_path != abs && abs.exists() {
    let _ = fs::remove_file(abs);   // 先删旧
}
fs::write(&final_path, content)?;   // 再写新，失败则旧数据已没
```
改标题触发重命名时旧文件先物理删除，`fs::write` 失败（磁盘满/占用）则提示词整篇丢失。对比 `rename_prompt`（store.rs:544-549）是先写新再删旧——两处顺序自相矛盾，是无心之笔。
**修**：先写新再删旧；并统一改原子写入（写 `.tmp` → `rename` 覆盖）。

### C2. 编辑中切换选中项无脏检查，未保存内容静默丢失
`src/App.svelte:688-691`、`201-206`
编辑态点击列表任意项或搜索框按 ↑↓，`editingBody/editingTitle` 直接被新内容覆盖，无确认。
**修**：引入 dirty 标志，切换前 confirm；或编辑态禁用列表切换。

### C3. 键盘导航 effect 反向劫持选中：保存后跳项、新建流程被破坏
`src/App.svelte:488-500`
`$effect` 持续用 selectedIndex 反写 selectedPath。三条已确认触发路径：
1. 保存后该项移到分类首位（后端按 updated 倒序），selectedIndex 旧槽位变别的项 → 保存完编辑器立刻跳到另一条 prompt；
2. 新建后 selectedIndex 未重置 → 直接加载旧项并强制 view 模式，磁盘留下空的时间戳文件，用户没进入编辑；
3. 同步完成触发 refresh → 重排 → 劫持 → 覆盖正在编辑的内容（与 C2 合流）。

**修**：index→path 对齐改为事件驱动（只在 ↑↓ 时同步），refresh 后以 selectedPath 为准反算 index；编辑态禁止该 effect 写 selectedPath。注意 `PromptList` 的 selectedIndex prop 目前没有任何视觉呈现，修完需加高亮。

---

## 二、HIGH

### 后端

**H1. `resolve_abs` 路径逃逸防护失效，可读写删任意文件** — `src-tauri/src/lib.rs:882-895`
设计意图"逃出 root 就拒绝"，但两处 fallback 都返回未校验的 `joined`：`read_prompt("../../../etc/passwd")` 可读 root 外文件；`save_prompt` 路径不存在时 canonicalize 必 Err → 可拼 `..` 写到任意位置；`delete_prompt` 可删任意文件。CSP 允许 jsdelivr 脚本（见 H8），供应链 XSS 即可借此打穿文件系统。
**修**：校验失败返回 `Err`，签名改 `Result<PathBuf, String>`。

**H2. 本地删除永不传播云端，已删提示词下载时"复活"** — `src-tauri/src/sync.rs:237-329`
push 只增不删，pull 以远程为准。本地删除后远程文件仍在，下次下载原样回来。sync.rs:9 注释声称"异步推送 DELETE"但代码没有这条路径，文档与实现脱节。
**修**：push 时对"远程有本地无"发 DELETE，或 pull 前记录 tombstone。

**H3. `.order.json` 上传了却永远下载不回来，排序同步名存实亡** — `src-tauri/src/sync.rs:273` vs `227-229`
push 特意上传 `.order.json`，pull 的 hidden 过滤把 `.` 开头全部丢弃；`.category-order.json` 连上传都没有。
**修**：两侧白名单对齐，放行 `.order.json` / `.category-order.json`。

**H4. 下载只比文件大小，静默覆盖本地未上传修改且无备份** — `src-tauri/src/sync.rs:96-109`、`433-451`
同字节数内容不同（改一个字删一个字）直接 skipped；大小不同直接覆盖，本地未 push 的编辑不进 `.trash`（清理多余文件反而有备份）。
**修**：覆盖前 rename 进 `.trash/`；判断条件加 mtime；报告提示"N 个文件大小相同未比对"。

**H5. 用户主动删除不可恢复** — `src-tauri/src/lib.rs:370-374` → `store.rs:604-606`
`delete_prompt` 直接物理删除；同步清理路径反而有 `.trash` 备份。配合 H2，云端也救不回来。
**修**：删除改为 rename 到 `.trash/{stem}_{timestamp}.md`。

### 前端

**H6. 编辑器分类下拉保存不生效（装饰性控件）** — `src/lib/Editor.svelte:163-172`、`App.svelte:263-284`、`types.ts:69-73`
`editingCategory` 没进 SaveRequest，前后端契约一致缺字段，用户改分类被静默丢弃。
**修**：SaveRequest 加 category 字段 + 后端处理目录移动；或移除该下拉。

**H7. PromptList 未实现 `onmounted` prop，键盘导航永不滚动列表** — `src/App.svelte:687` vs `src/lib/PromptList.svelte:7-28`
`scrollToIndexFn` 永远 null，`scrollIntoView()` 静默空转。`svelte-check` 已报此错。
**修**：声明该 prop 并回传滚动函数。

**H8. 远程脚本无 SRI 校验，供应链单点失陷即特权 webview RCE** — `src/lib/renderers/loadRemote.ts:71-72`
mermaid/KaTeX/highlight 三库从 jsdelivr 注入，无 integrity 哈希。CSP 限定 jsdelivr 是缓解，但源失陷时任意 JS 进入特权 webview，可 invoke 全部 Rust 命令（文件读写、凭据）。版本固定防漂移不防失陷。
**修（推荐）**：三库打包进本地 bundle（桌面应用本就离线优先，顺带消灭 CDN 可用性问题）；次选 UMD 加 SRI + crossorigin。CSP 同步收紧 script-src 去掉 CDN。

**H9. Editor 的 renderRich `$effect` 漏依赖 body，切换提示词后 mermaid/KaTeX/高亮不渲染** — `src/lib/Editor.svelte:82-85`
effect 只读 `previewEl` 和 `mode`，view→view 切换 prompt 时依赖不变，`{@html}` 更新了 DOM 但增强层不重跑——新内容停留在 mermaid 源码占位、`$...$` 不渲染、代码无高亮。**这是最高频主路径，两路审查独立发现。**
**修**：effect 内显式读取 `body` 纳入依赖。

**H10. 对话框内按 Esc 穿透：关弹窗的同时隐藏整个窗口** — `src/App.svelte:766`、`830`、`502-519` 等 4 处入口
焦点在对话框 input 按 Esc → 冒泡关对话框 → 继续冒泡到 window handler → `hideWindow()`。
**修**：各 Esc 处理加 `e.stopPropagation()`。

---

## 三、MEDIUM（择要，完整 12 条略）

| # | 问题 | 位置 |
|---|---|---|
| M1 | 全模块无原子写入，崩溃/断电即半截文件（统一"临时文件+rename"helper） | store.rs:430/544/312、lib.rs:178、sync.rs:344/450 |
| M2 | `rename_category` 合并先删目标再 rename，失败丢文件；同名覆盖无确认 | store.rs:590-592 |
| M3 | 同步并发防护 TOCTOU + panic 后 syncing 永久卡死（单次锁内 check-and-set + drop guard） | lib.rs:754-763 |
| M4 | 同步期间不阻塞本地保存，下载与用户编辑互相覆盖 | sync.rs 全局 |
| M5 | keyring 不可用时整个配置保存失败（应降级存非密字段） | lib.rs:151-160 |
| M6 | WebDAV URL 未编码，`#`/`?` 标题可能上传失败（需真机验证） | sync.rs:308 |
| M7 | macOS `window.confirm` 可能静默 false，删除确认失效（换 plugin-dialog 的 ask，需验证） | App.svelte:309/420 |
| M8 | pointer 拖拽无 setPointerCapture、move 不查 buttons，拖出窗口松手卡死 + 陈旧索引误提交 | PromptList.svelte:45-75、CategoryTabs.svelte:64-94 |
| M9 | 类型检查红且无门禁：tsc 4 错 + svelte-check 6 错，build 不卡类型、CI 无 check（H7 正是漏网实例） | package.json、ci.yml |
| M10 | 右键菜单无视口边界检测，底部右键溢出不可见 | ContextMenu.svelte:73-76 |
| M11 | search.ts 注释声称搜正文，实现不搜（Prompt 类型无 body 字段，改注释或实现） | search.ts:9 vs :14-20 |
| M12 | 测试无 npm script 入口（CI 有命令但本地 `npm test` 不存在） | package.json |

---

## 四、LOW（择要，完整 22 条略）

- `__KEEP__` 哨兵值与用户真实密码碰撞（前后端同一字符串，改 `Option<String>`）— lib.rs:729、Settings.svelte:120
- `dirs = "5"` 未使用依赖；`app_config_dir` 失败静默回退 `./` — Cargo.toml:31、lib.rs:117
- 图片 token 未做协议白名单（`![x](javascript:...)` 原样输出，WebView 不可利用，纵深防御缺口）— markdown.ts:61
- `importEsm` 探测重复下载 mermaid ~3MB（改 HEAD 或删探测）— loadRemote.ts:35-41
- `loadPromptContent` 竞态：快速切换旧响应覆盖新内容（加请求序号）— App.svelte:201-223
- 拖拽手势中 refresh 替换列表导致 from/to 索引错位 — App.svelte:189-197
- 编辑态 Esc 直接隐藏窗口（建议先退出编辑）；重命名/新建分类输入框均无 autofocus
- tsconfig：`checkJs: true` 但 include 无 .js（死配置）、缺 `noEmit: true`
- 未捕获 rejection 多处；`savePrompt` 双发 copy_mode/copyMode 死代码
- Linux/Wayland 下 enigo 模拟粘贴不可用、macOS 需辅助功能权限，失败无引导 — lib.rs

---

## 五、确认做得对的（排除误报）

- **凭据安全属实**：WebDAV 密码走 keyring 系统凭据库，config.json 不落明文，有 legacy 迁移 + 测试（lib.rs:1292-1364），README 声称属实。
- **XSS 防线扎实**：`{@html}` 全项目仅 1 处，上游 markdown.ts raw HTML 全转义 + 链接协议白名单（实跑探测实体转义绕过也被挡）；mermaid `securityLevel:"strict"`；KaTeX `trust:false`。
- `open_url` 协议白名单防 `file://`/`javascript:` 注入（lib.rs:870-876 + 测试）。
- pull 远程列表为空时拒绝清理本地（sync.rs:116-121），防解析失败全量误删——好的防御。
- capabilities 权限最小化；Mutex 全部处理 poison；`reorderInFlight/pendingRefresh` 写盘期护栏思路正确。
- 测试是真断言（XSS 套件、拖拽边界、storage 兜底），zh/en 键一致性靠类型强制。
- CI 三平台跑前端单测 + cargo test + Tauri 构建，测试挂了不浪费 build。

## 六、总体评价

**架构没有方向性问题**：分层清晰（命令/存储/同步），本地 Markdown + 手动双向同步选型符合坚果云限速现实，注释质量高于平均，安全意识到位。问题集中在三个系统性模式：

1. **数据丢失风险 = "非原子写 + 删除无备份"两个模式的排列组合**（C1/H5/M1）。一个"临时文件+rename"helper + "删除先进 .trash"约定可消掉大半。
2. **同步模块是按"上传"视角写的，下载方向是后补的，两侧契约没对齐**（H2/H3/H4）。需要一次整体对账而非逐个打补丁。
3. **前端选中状态管理是最大设计弱点**：`selectedIndex ↔ selectedPath` 双向互驱 + 无脏检查，C2/C3 同源。重构为"selectedPath 唯一真相，index 只是键盘导航的瞬态指针"。

另有一条组合攻击链值得单独点名：**H1（路径逃逸假防护）+ H8（CDN 无 SRI）= 供应链 XSS → 任意文件删除**。单看各自都中等偏高，组合后是安全上最该先堵的一对。

## 修复优先级建议

| 批次 | 内容 | 预估 |
|---|---|---|
| P0 数据丢失 | C1+C2+C3、H5、M1 原子写 helper | 1-2 天 |
| P0 安全链 | H1 resolve_abs 返 Err、H8 三库本地化（顺手收紧 CSP） | 1 天 |
| P1 同步对账 | H2+H3+H4 一起改，补 tombstone 或镜像删除 | 1-2 天 |
| P1 主路径 | H9 effect 依赖、H7 onmounted、H10 Esc 穿透、H6 分类下拉 | 半天 |
| P2 质量门 | M9 修类型 + CI 加 svelte-check、M12 加 npm test | 1 小时 |
